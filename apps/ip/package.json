{
  "name": "ip",
  "version": "1.0.0",
  "description": "IP info API",
  "main": "./.output/server/index.mjs",
  "private": true,
  "scripts": {
    "dev": "npx nitropack dev",
    "build": "npx nitropack build",
    "preview": "node .output/server/index.mjs",
    "deploy": "pnpm build && op run wrangler publish"
  },
  "dependencies": {
    "nitropack": "latest",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230115.0",
    "wrangler": "3.1.1"
  }
}
